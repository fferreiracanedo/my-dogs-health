import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  font: {
    heading: 'Montserrat',
    body: 'Inter',
  },

  color: {
    1: '#FED7D7',
  },
});

export default theme;
